const express = require("express");
const router = express.Router();

// Placeholder for supplier routes
router.get("/", (req, res) => {
  res.json({ success: true, message: "Supplier routes - coming soon" });
});

module.exports = router;
