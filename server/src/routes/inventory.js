const express = require("express");
const router = express.Router();

// Placeholder for inventory routes
router.get("/", (req, res) => {
  res.json({ success: true, message: "Inventory routes - coming soon" });
});

module.exports = router;
