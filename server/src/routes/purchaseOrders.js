const express = require("express");
const router = express.Router();

// Placeholder for purchase order routes
router.get("/", (req, res) => {
  res.json({ success: true, message: "Purchase Order routes - coming soon" });
});

module.exports = router;
